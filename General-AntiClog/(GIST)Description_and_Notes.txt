|GIST BETA

TODO:

1. Better approach on sorting out items?
2. Starting the cache system for ordering as well...
3. For the barebones variant made, this could just be CST at this rate... or a dedicated one ig...
4. The extra proc will basically fill in the rest of the QOL stuff as it acesses the cached extra stuff w/in the main proc...


---

Extra Proc Design Docs:
- The main proc will stash in all of the variables of the original. 
- It primarily checks if the stash is still valid, does the rss checks for power, and does the origin system stuff...
    - First, it takes in the available storages and binds it does have..., and checks for validity so it automates the process (It has full control on the storage check)
    - It loops through, and sees
    - From said cache, it takes in the recognized power bind, and does RSS
    - Uncertain if it also handles origin checks... but it does handle feeding the reference in at least...
    - Will act as the main configuration and doesn't take away from the main proc on handling it
    - It also sends out messages similar to S-AC.
    - You can tell it to force a refresh using and binded sorter it does have on hand.
    - Speaking of said sorter, 
- The main processor (IST - Inter-Storage Transporter) can work independently of the config processor (The General).
- All together they form GIST (General Inter-Storage Transport).
    - Heh, I love double meanings.

---

Doing the same trick of reverse arrays for unloaders, config items, etc. in the build loop
- Requires knowledge on the stuff is still "valid"
- Since something spooky happens when unbinding..., as the system will keep controlling it, and any new binds aren't updated or recognized in

Additional Micro-Proc That handles immediate bitConfigurations
this technically allows to "slow down" transmission if needed?

Major Rewrites

Multi-Origin requires the ff.
Figure Out which unloader is connected to which container (measure by distance?)
May be complicated
using the array system, you can now dedicate certain unloaders to a different config system...
may hit perf hard though...

Do not add stuff unless it is something that can work.

Goal: 0.5-0.6s perf.

Multi-Proc system
This is fairly tricky, but im going to do basically is transmit the recorded storages, through the system?

The main situation is basically, multi-origin with one at core, and another at a seperate storage
They merge into one md? And along the way the way, one md is hooked up to an overdrive system (micro proc will handle the necessary configutations)
And the final distination only requires silicon but no phase... (so need to prevent phase from trickling down the system).

This is technically possible from the read and write commands, as its now possible to transmit building references.